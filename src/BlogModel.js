export default class Blog {
    constructor(blog,author,categories,authorID) {
      this.blog = blog;
      this.author = author;
      this.categories=categories
      this.authorID=authorID
    }
  }